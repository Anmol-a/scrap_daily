name: Weekly Spec Fill

on:
  schedule:
    - cron: '30 21 * * 0'   # 3:00 AM IST every Sunday (1hr after weekly new products)
  workflow_dispatch:

jobs:
  specs-fill:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Chrome (latest stable, version auto-detected by scraper)
        run: |
          wget -q https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
          sudo apt install -y ./google-chrome-stable_current_amd64.deb
          echo "Chrome version: $(google-chrome --version)"

      - name: Install Python dependencies
        run: |
          pip install selenium undetected-chromedriver supabase python-dotenv requests

      - name: Run specs filler
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          AFFILIATE_TAG: sync8in-21
        run: |
          python3 scrapers/scraper.py specs

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: specs-logs-${{ github.run_number }}
          path: logs/
          retention-days: 7